For the messaging system to work, there needs to be a .env file in the root folder for the vite app and it should have the following

VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
